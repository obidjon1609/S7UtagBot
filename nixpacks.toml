[phases.build]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.start]
cmd = "python3 pro.tag.10.py"

[phases.install]
cmds = ["apt-get update && apt-get install -y --no-install-recommends python3-dev python3-pip"]

[variables]
PORT = "8080"
